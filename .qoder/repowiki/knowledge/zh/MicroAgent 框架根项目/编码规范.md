- 所有异步 API 使用 async/await，测试通过 pytest-asyncio 的 asyncio_mode=auto 自动处理事件循环
- 工具函数统一通过装饰器注册到 ToolRegistry，并以 ToolResult.ok/error 返回结果
- 配置类基于 Pydantic v2，通过 from_config 类方法构造实例
- 扩展点采用 Protocol 抽象（PreLLMHook/ToolHook/ContextSource），实现零开销注入
- 测试按 unit/integration/benchmark 分层，integration 测试通过 @pytest.mark.integration 标记并受环境变量控制