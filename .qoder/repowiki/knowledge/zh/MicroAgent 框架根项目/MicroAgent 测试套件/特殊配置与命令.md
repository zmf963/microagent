单元测试直接 `pytest tests/unit/` 运行；集成测试需设置 `MICROAGENT_TEST_BASE_URL`、`MICROAGENT_TEST_API_KEY`、`MICROAGENT_TEST_MODEL` 三个环境变量后通过 `pytest tests/integration/ -v` 执行；基准测试需显式指定 `--benchmark` 或直接运行 `pytest tests/benchmark/ -v -s`，否则被 `pytest.mark.benchmark` 跳过。